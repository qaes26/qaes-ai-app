# استخدم صورة أساسية (Base Image) تحتوي على Python
FROM python:3.9-slim-buster

# اجعل /app هو دليل العمل داخل الحاوية
WORKDIR /app

# انسخ ملف المتطلبات إلى دليل العمل
COPY requirements.txt .

# قم بتثبيت المتطلبات
RUN pip install --no-cache-dir -r requirements.txt

# انسخ بقية ملفات التطبيق إلى دليل العمل
COPY . .

# تحديد المنفذ الذي سيستمع إليه التطبيق (إذا كان تطبيق ويب)
# PORT 8000 # مثال إذا كان تطبيقك يعمل على هذا المنفذ

# الأمر الذي سيتم تشغيله عند بدء تشغيل الحاوية
# هذا الأمر يعتمد على كيفية تشغيل تطبيقك. إليك بعض الأمثلة الشائعة:
# 1. إذا كنت تستخدم Flask و Gunicorn (موصى به للإنتاج):
# CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:8000", "app:app"]
# (استبدل app:app باسم ملفك الرئيسي واسم كائن التطبيق، مثلاً my_app:app)

# 2. إذا كنت تشغل ملف Python مباشرة:
# CMD ["python", "app.py"]

# 3. إذا كان تطبيقك يستخدم FastAPI / Uvicorn:
# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
# (استبدل main:app باسم ملفك الرئيسي واسم كائن التطبيق)