# 1. ابدأ من صورة بايثون رسمية وخفيفة
FROM python:3.9-slim

# 2. قم بإنشاء مجلد عمل داخل الصندوق
WORKDIR /app

# 3. انسخ ملف المتطلبات أولاً (للاستفادة من التخزين المؤقت)
COPY requirements.txt .

# 4. قم بتثبيت كل المكتبات المطلوبة
RUN pip install --no-cache-dir -r requirements.txt

# 5. انسخ كل ملفات المشروع الباقية إلى داخل الصندوق
COPY . .

# 6. أخبر Docker أن التطبيق سيعمل على المنفذ 5000 داخل الصندوق
EXPOSE 5000

# 7. الأمر النهائي لتشغيل الخادم عند بدء تشغيل الصندوق
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]